#include <stdio.h>

int main() {
    // Type your code below
    int b1 = 0;
    int b2 = 0;
    int b3 = !((b1 + b2) > (b1 * b2));
    
    // Don't change the line below
    printf("b3 = %d\n", b3);
    
    return 0;
}